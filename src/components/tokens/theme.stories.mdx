import { Meta, Story, Preview } from '@storybook/addon-docs/blocks'

import Theme from './ThemeItem'
import theme from '../../assets/tokens/themes/dark.raw.json'
import groupBy from 'lodash/groupBy'

<Meta title="Core|Theme" component={Theme} />

# Theme

Let's define a story for our `Theme` component:

<Story name="all">
  {{
    components: { Theme },
    template: '<theme />'
  }}
</Story>
